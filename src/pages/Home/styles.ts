import styled from 'styled-components'

export const Wrapper = styled.div`
  max-width: 112rem;
  margin: 0 auto;

  > div {
    display: flex;
    align-items: center;
    gap: 0.8rem;
    margin-top: -2.6rem;
  }
`
